"use client";

// Dummy Facebook Pixel service that does nothing
// This is a placeholder to avoid breaking imports in other files

export const FB_PIXEL_ID = "";

export default function PixelEvents() {
  return null;
}

export const pageview = () => {};

export const event = () => {};

export const sendEvent = () => {};

export const sendSeverEvent = async () => {}; 