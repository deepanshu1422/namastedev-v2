/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.freepik.com", "assets-global.website-files.com", "i.ibb.co", "images.ctfassets.net"]
    }
};

export default nextConfig;
